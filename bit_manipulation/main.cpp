#include <iostream>
#include "helper_functions.h"
using namespace std;

int main()
{   
    /*
    int inp, bit;
    char arr[3] = {'a', 'b', 'c'};
    cout << "Is this number a power of 2? : ";
    cin >> inp;
	if(isPowerof2(inp))
        cout << "Yes\n";
    else
        cout << "No\n";

    cout << "Find number of 1s in binary rep. of this num:  ";
    cin >> inp;
    cout << numOfones(inp) << endl;

    cout << "Enter num and ith bit to check if it is set: ";
    cin >> inp >> bit;
    isIthBitSet(inp, bit);

    possibleSubsets(arr, 3);
    */

    cout << largestPowerof2(45) << endl;


}
